name: agentlocate
channels:
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - --extra-index-url https://download.pytorch.org/whl/cu124
      - torch==2.5.1+cu124
      - torchvision==0.20.1+cu124
      - torchaudio==2.5.1+cu124
      - openai>=1.0
      - transformers>=4.45
      - accelerate>=0.34
      - bitsandbytes>=0.46.1
      - sentencepiece>=0.2
      - datasets>=2.21
      - peft>=0.13
      - pyyaml>=6.0
